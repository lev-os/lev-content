# Source Material (Not Public Site Content)

This folder stores source material used to curate public docs.

- `skills/`: mirrored `SKILL.md` files from `~/.agents/skills`
- `internal-docs/`: mirrored internal docs from `leviathan/docs/`

Public Astro docs are served only from `docs/public/`.
